/**
 * ExecuteGetScoreExplanation - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: scoring
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteGetScoreExplanation Use Case
 *
 * DDD: Application use case for get Explanation.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { GetScoreExplanationInput, GetScoreExplanationOutput } from "../dto/explanation.dto.js";
import type { ExecutionContextService, IdGeneratorService } from "@loadline/services/_shared/index.js";
import type { ExplanationRepository } from "../ports/index.js";
import { NotFoundError, ValidationError } from "../errors/index.js";

export class ExecuteGetScoreExplanation {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly explanation: ExplanationRepository
  ) {}

  async execute(input: GetScoreExplanationInput): Promise<GetScoreExplanationOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.scoId();

    // All field names extracted from OpenAPI spec: none
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).scoreId) {
      throw new ValidationError("scoreId is required");
    }
    const scoreId = (input as any).scoreId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.explanation.getScoreExplanation({ scoreId: scoreId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${scoreId}`);
    }

    return entity as GetScoreExplanationOutput;
  }
}
