/**
 * ExecuteDisableTenantUser - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: identity
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteDisableTenantUser Use Case
 *
 * DDD: Application use case for update Disable.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { DisableTenantUserInput, DisableTenantUserOutput } from "../dto/disable.dto.js";
import type { ExecutionContextService, IdGeneratorService } from "@loadline/services/_shared/index.js";
import type { DisableRepository } from "../ports/index.js";
import { PermissionDeniedError, ValidationError } from "../errors/index.js";
import { CanUpdate } from "../policies/can-update.policy.js";
export class ExecuteDisableTenantUser {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly disable: DisableRepository
  ) {}

  async execute(input: DisableTenantUserInput): Promise<DisableTenantUserOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.idnId();

    // All field names extracted from OpenAPI spec
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).userId) {
      throw new ValidationError("userId is required");
    }
    const userId = (input as any).userId;
    // No get/list operation for this resource - call update directly
    // Check policy
    if (!CanUpdate.allow({} as any, "")) {
      throw new PermissionDeniedError("Operation not allowed");
    }

    // Update entity via repository (pass correlation ID for response meta)
    const updated = await this.disable.disableTenantUser({ ...input, orgId: this.context.getOrgId(), correlationId } as any);

    return updated as DisableTenantUserOutput;
  }
}
