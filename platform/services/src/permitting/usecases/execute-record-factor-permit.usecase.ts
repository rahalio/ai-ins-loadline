/**
 * ExecuteRecordFactorPermit - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: permitting
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteRecordFactorPermit Use Case
 *
 * DDD: Application use case for get FactorPermit.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { RecordFactorPermitInput, RecordFactorPermitOutput } from "../dto/factor-permit.dto.js";
import type { ExecutionContextService, IdGeneratorService } from "@loadline/services/_shared/index.js";
import type { FactorPermitRepository } from "../ports/index.js";
import { NotFoundError, ValidationError } from "../errors/index.js";

export class ExecuteRecordFactorPermit {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly factorPermit: FactorPermitRepository
  ) {}

  async execute(input: RecordFactorPermitInput): Promise<RecordFactorPermitOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.prmId();

    // All field names extracted from OpenAPI spec: conditions, dataCategory, explainableToApplicant, factor, jurisdiction, officerId, position, proxyRiskAssessment, restrictedHealthDataBasis
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).officerId) {
      throw new ValidationError("officerId is required");
    }
    const officerId = (input as any).officerId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.factorPermit.recordFactorPermit({ officerId: officerId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${officerId}`);
    }

    return entity as RecordFactorPermitOutput;
  }
}
