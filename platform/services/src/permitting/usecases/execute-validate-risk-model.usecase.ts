/**
 * ExecuteValidateRiskModel - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: permitting
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteValidateRiskModel Use Case
 *
 * DDD: Application use case for get Validation.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { ValidateRiskModelInput, ValidateRiskModelOutput } from "../dto/validation.dto.js";
import type { ExecutionContextService, IdGeneratorService } from "@loadline/services/_shared/index.js";
import type { ValidationRepository } from "../ports/index.js";
import { NotFoundError, ValidationError } from "../errors/index.js";

export class ExecuteValidateRiskModel {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly validation: ValidationRepository
  ) {}

  async execute(input: ValidateRiskModelInput): Promise<ValidateRiskModelOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.prmId();

    // All field names extracted from OpenAPI spec: blockedFactors, blockedJurisdictions, conditions, outcome, permittedJurisdictions, stabilityAssessment, validatedBy
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).modelId) {
      throw new ValidationError("modelId is required");
    }
    const modelId = (input as any).modelId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.validation.validateRiskModel({ modelId: modelId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${modelId}`);
    }

    return entity as ValidateRiskModelOutput;
  }
}
