/**
 * ExecuteRecordTouchpointEvent - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: engagement
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteRecordTouchpointEvent Use Case
 *
 * DDD: Application use case for get Touchpoint.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { RecordTouchpointEventInput, RecordTouchpointEventOutput } from "../dto/touchpoint.dto.js";
import type { ExecutionContextService, IdGeneratorService } from "@loadline/services/_shared/index.js";
import type { TouchpointRepository } from "../ports/index.js";
import { NotFoundError, ValidationError } from "../errors/index.js";

export class ExecuteRecordTouchpointEvent {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly touchpoint: TouchpointRepository
  ) {}

  async execute(input: RecordTouchpointEventInput): Promise<RecordTouchpointEventOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.engId();

    // All field names extracted from OpenAPI spec: consentRecordId, enrolmentId, occurredAt, touchpointKind
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).consentRecordId) {
      throw new ValidationError("consentRecordId is required");
    }
    const consentrecordId = (input as any).consentRecordId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.touchpoint.recordTouchpointEvent({ consentRecordId: consentrecordId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${consentrecordId}`);
    }

    return entity as RecordTouchpointEventOutput;
  }
}
