/**
 * ExecuteGetTouchpointFrequency - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: engagement
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteGetTouchpointFrequency Use Case
 *
 * DDD: Application use case for get Frequency.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { GetTouchpointFrequencyInput, GetTouchpointFrequencyOutput } from "../dto/frequency.dto.js";
import type { ExecutionContextService, IdGeneratorService } from "@loadline/services/_shared/index.js";
import type { FrequencyRepository } from "../ports/index.js";
import { NotFoundError, ValidationError } from "../errors/index.js";

export class ExecuteGetTouchpointFrequency {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly frequency: FrequencyRepository
  ) {}

  async execute(input: GetTouchpointFrequencyInput): Promise<GetTouchpointFrequencyOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.engId();

    // All field names extracted from OpenAPI spec: none
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).enrolmentId) {
      throw new ValidationError("enrolmentId is required");
    }
    const enrolmentId = (input as any).enrolmentId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.frequency.getTouchpointFrequency({ enrolmentId: enrolmentId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${enrolmentId}`);
    }

    return entity as GetTouchpointFrequencyOutput;
  }
}
