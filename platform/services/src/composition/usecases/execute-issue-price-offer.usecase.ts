/**
 * ExecuteIssuePriceOffer - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: composition
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteIssuePriceOffer Use Case
 *
 * DDD: Application use case for get Offer.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { IssuePriceOfferInput, IssuePriceOfferOutput } from "../dto/offer.dto.js";
import type { ExecutionContextService, IdGeneratorService } from "@loadline/services/_shared/index.js";
import type { OfferRepository } from "../ports/index.js";
import { NotFoundError, ValidationError } from "../errors/index.js";

export class ExecuteIssuePriceOffer {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly offer: OfferRepository
  ) {}

  async execute(input: IssuePriceOfferInput): Promise<IssuePriceOfferOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.cmpId();

    // All field names extracted from OpenAPI spec: none
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).compositionId) {
      throw new ValidationError("compositionId is required");
    }
    const compositionId = (input as any).compositionId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.offer.issuePriceOffer({ compositionId: compositionId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${compositionId}`);
    }

    return entity as IssuePriceOfferOutput;
  }
}
