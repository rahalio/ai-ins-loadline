/**
 * ExecuteSettleClaimAutomatically - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: claims
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteSettleClaimAutomatically Use Case
 *
 * DDD: Application use case for update AutomaticSettlement.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { SettleClaimAutomaticallyInput, SettleClaimAutomaticallyOutput } from "../dto/automatic-settlement.dto.js";
import type { ExecutionContextService, IdGeneratorService } from "@loadline/services/_shared/index.js";
import type { AutomaticSettlementRepository } from "../ports/index.js";
import { PermissionDeniedError, ValidationError } from "../errors/index.js";
import { CanUpdate } from "../policies/can-update.policy.js";
export class ExecuteSettleClaimAutomatically {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly automaticSettlement: AutomaticSettlementRepository
  ) {}

  async execute(input: SettleClaimAutomaticallyInput): Promise<SettleClaimAutomaticallyOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.clmId();

    // All field names extracted from OpenAPI spec
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).id) {
      throw new ValidationError("id is required");
    }
    const idValue = (input as any).id;
    // No get/list operation for this resource - call update directly
    // Check policy
    if (!CanUpdate.allow({} as any, "")) {
      throw new PermissionDeniedError("Operation not allowed");
    }

    // Update entity via repository (pass correlation ID for response meta)
    const updated = await this.automaticSettlement.settleClaimAutomatically({ ...input, orgId: this.context.getOrgId(), correlationId } as any);

    return updated as SettleClaimAutomaticallyOutput;
  }
}
