export function ScoringWorkbench() {
  return (
    <section>
      <p className="eyebrow">Underwriting</p>
      <h1>Scoring workbench</h1>
      <p className="lead">
        Consent at point of use, factor-level explanation mapped to the rating basis, and blocked features
        when permits or restricted health data stand in the way.
      </p>
      <div className="panel">Wire to generated `@/services/domains/scoring` clients after codegen.</div>
    </section>
  );
}
