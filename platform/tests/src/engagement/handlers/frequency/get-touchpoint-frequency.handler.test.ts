/**
 * GetTouchpointFrequency handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: engagement
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { getTouchpointFrequency } from "@loadline/api-server/domains/engagement/handlers";
import type { EngagementDDDDependencies } from "@loadline/api-server/domains/engagement";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@loadline/tests/shared";
import { ENGAGEMENT_USE_CASES } from "../__factories__/use-cases.js";

describe("GetTouchpointFrequency", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: EngagementDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<EngagementDDDDependencies>(
      ENGAGEMENT_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity when found", async () => {
    const entity = { id: 'mock-id', orgId } as any;
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeGetTouchpointFrequency, "execute").mockResolvedValue(useCaseResult);
    mockRequest.params = { enrolmentId: entity.id };

    await getTouchpointFrequency(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeGetTouchpointFrequency.execute).toHaveBeenCalledWith(expect.objectContaining({ enrolmentId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

  it("should return error when entity not found", async () => {
    const nonExistentId = "non-existent-id";
    mockRequest.params = { enrolmentId: nonExistentId };
    
    vi.spyOn(mockDeps.executeGetTouchpointFrequency, "execute").mockRejectedValue(
      new Error("Entity not found")
    );

    await expect(
      getTouchpointFrequency(mockRequest, mockReply, mockDeps)
    ).rejects.toThrow();
  });

});
