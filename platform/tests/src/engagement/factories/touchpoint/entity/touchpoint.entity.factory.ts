/**
 * Touchpoint Entity Factory
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: engagement
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { faker } from "@faker-js/faker";
import type { TouchpointEntity } from "@loadline/core/engagement/models/index.js";
import { engagementSchemas } from "@loadline/core/engagement";
import { ulid } from "ulid";

// Domain prefix from OpenAPI spec info.x-domain
const DOMAIN_PREFIX = "eng";

export function createTouchpoint(overrides: Partial<TouchpointEntity> = {}) {
  const raw = {
    id: overrides.id ?? `${DOMAIN_PREFIX}_${ulid().toLowerCase()}`,
    createdAt: overrides.createdAt ?? new Date().toISOString(),
    updatedAt: overrides.updatedAt ?? new Date().toISOString(),
    enrolmentId: overrides.enrolmentId ?? faker.string.uuid(),
    occurredAt: overrides.occurredAt ?? new Date().toISOString(),
    touchpointKind: overrides.touchpointKind ?? "data_sync",
    ...overrides,
  };

  return engagementSchemas.TouchpointEvent.parse(raw);
}

export function touchpointBatch(n = 3, overrides = {}) {
  return Array.from({ length: n }, () => createTouchpoint(overrides));
}
