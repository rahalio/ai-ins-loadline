/**
 * GetExpenseMeasurement handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: claims
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { getExpenseMeasurement } from "@loadline/api-server/domains/claims/handlers";
import type { ClaimsDDDDependencies } from "@loadline/api-server/domains/claims";
import { createExpenseMeasurement as createExpenseMeasurementEntity } from "@loadline/factories/claims";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@loadline/tests/shared";
import { CLAIMS_USE_CASES } from "../__factories__/use-cases.js";

describe("GetExpenseMeasurement", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: ClaimsDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<ClaimsDDDDependencies>(
      CLAIMS_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity", async () => {
    const entity = createExpenseMeasurementEntity({ orgId });
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeGetExpenseMeasurement, "execute").mockResolvedValue(useCaseResult);

    await getExpenseMeasurement(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeGetExpenseMeasurement.execute).toHaveBeenCalledWith(expect.anything());
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

});
