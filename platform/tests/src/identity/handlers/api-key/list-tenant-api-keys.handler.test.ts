/**
 * ListTenantApiKeys handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: identity
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { listTenantApiKeys } from "@loadline/api-server/domains/identity/handlers";
import type { IdentityDDDDependencies } from "@loadline/api-server/domains/identity";
import { createApiKey as createApiKeyEntity } from "@loadline/factories/identity";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@loadline/tests/shared";
import { IDENTITY_USE_CASES } from "../__factories__/use-cases.js";

describe("ListTenantApiKeys", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: IdentityDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<IdentityDDDDependencies>(
      IDENTITY_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with list of entities", async () => {
    const entities = [
      createApiKeyEntity({ orgId }),
      createApiKeyEntity({ orgId }),
    ];
    const useCaseResult = {
      data: { items: entities, nextCursor: undefined, prevCursor: undefined },
      meta: { correlationId: "test", timestamp: new Date().toISOString(), pagination: {} }
    };
    
    vi.spyOn(mockDeps.executeListTenantApiKeys, "execute").mockResolvedValue(useCaseResult);

    await listTenantApiKeys(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeListTenantApiKeys.execute).toHaveBeenCalledWith(expect.anything());
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

});
