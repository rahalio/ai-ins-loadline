/**
 * IssuePriceOffer handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: composition
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { issuePriceOffer } from "@loadline/api-server/domains/composition/handlers";
import type { CompositionDDDDependencies } from "@loadline/api-server/domains/composition";
import { createOffer as createOfferEntity } from "@loadline/factories/composition";
import { createCreateOfferRequest } from "@loadline/factories/composition";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@loadline/tests/shared";
import { COMPOSITION_USE_CASES } from "../__factories__/use-cases.js";

describe("IssuePriceOffer", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: CompositionDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<CompositionDDDDependencies>(
      COMPOSITION_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 201 with created entity", async () => {
    const input = createCreateOfferRequest();
    const entity = createOfferEntity({ orgId });
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    mockRequest.body = input;
    vi.spyOn(mockDeps.executeIssuePriceOffer, "execute").mockResolvedValue(useCaseResult);

    await issuePriceOffer(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeIssuePriceOffer.execute).toHaveBeenCalledWith(expect.anything());
    expect(mockReply.code).toHaveBeenCalledWith(201);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

});
