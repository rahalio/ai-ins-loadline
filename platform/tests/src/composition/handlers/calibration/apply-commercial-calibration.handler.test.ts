/**
 * ApplyCommercialCalibration handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: composition
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { applyCommercialCalibration } from "@loadline/api-server/domains/composition/handlers";
import type { CompositionDDDDependencies } from "@loadline/api-server/domains/composition";
import { createCalibration as createCalibrationEntity } from "@loadline/factories/composition";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@loadline/tests/shared";
import { COMPOSITION_USE_CASES } from "../__factories__/use-cases.js";

describe("ApplyCommercialCalibration", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: CompositionDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<CompositionDDDDependencies>(
      COMPOSITION_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 201 with created entity", async () => {
    const input = {} as any;
    const entity = createCalibrationEntity({ orgId });
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    mockRequest.body = input;
    vi.spyOn(mockDeps.executeApplyCommercialCalibration, "execute").mockResolvedValue(useCaseResult);

    await applyCommercialCalibration(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeApplyCommercialCalibration.execute).toHaveBeenCalledWith(expect.anything());
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

});
