/**
 * SetVariationEnvelope handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: composition
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { setVariationEnvelope } from "@loadline/api-server/domains/composition/handlers";
import type { CompositionDDDDependencies } from "@loadline/api-server/domains/composition";
import { createVariationEnvelope as createVariationEnvelopeEntity } from "@loadline/factories/composition";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@loadline/tests/shared";
import { COMPOSITION_USE_CASES } from "../__factories__/use-cases.js";

describe("SetVariationEnvelope", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: CompositionDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<CompositionDDDDependencies>(
      COMPOSITION_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with updated entity", async () => {
    const entity = createVariationEnvelopeEntity({ orgId });
    const input = {} as any;
    const updatedEntity = { ...entity, ...input };
    const useCaseResult = { data: updatedEntity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    mockRequest.params = { id: entity.id };
    mockRequest.body = input;
    vi.spyOn(mockDeps.executeSetVariationEnvelope, "execute").mockResolvedValue(useCaseResult);

    await setVariationEnvelope(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeSetVariationEnvelope.execute).toHaveBeenCalledWith(expect.objectContaining({ id: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

});
