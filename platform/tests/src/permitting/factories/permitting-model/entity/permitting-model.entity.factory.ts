/**
 * PermittingModel Entity Factory
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: permitting
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { faker } from "@faker-js/faker";
import type { PermittingModelEntity } from "@loadline/core/permitting/models/index.js";
import { permittingSchemas } from "@loadline/core/permitting";
import { ulid } from "ulid";

// Domain prefix from OpenAPI spec info.x-domain
const DOMAIN_PREFIX = "prm";

export function createPermittingModel(overrides: Partial<PermittingModelEntity> = {}) {
  const raw = {
    id: overrides.id ?? `${DOMAIN_PREFIX}_${ulid().toLowerCase()}`,
    createdAt: overrides.createdAt ?? new Date().toISOString(),
    updatedAt: overrides.updatedAt ?? new Date().toISOString(),
    name: overrides.name ?? faker.person.fullName(),
    premiumTerm: overrides.premiumTerm ?? "expected_claims",
    state: overrides.state ?? "candidate",
    ...overrides,
  };

  return permittingSchemas.RiskModel.parse(raw);
}

export function permittingModelBatch(n = 3, overrides = {}) {
  return Array.from({ length: n }, () => createPermittingModel(overrides));
}
