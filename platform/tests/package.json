{
  "name": "@loadline/tests",
  "version": "0.1.0",
  "private": true,
  "description": "Vitest shared helpers for DDD Codegen Starter platform tests",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "@loadline/core": "workspace:*",
    "@loadline/services": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vitest": "^2.1.9"
  }
}
